Toggle TTS (text-to-speech) for Claude Code responses.

## Steps

1. Check if `~/.claude-tts` exists
2. If it exists, delete it and report "TTS OFF"
3. If it doesn't exist, create it and report "TTS ON"

Use the Bash tool to check and toggle the file. Report the result in one line.
