#!/bin/bash
brew install git git-lfs
git config --global user.name "hidekuma"
git config --global user.email "d.hidekuma@gmail.com"
git config --global core.precomposeunicode true
git config --global core.quotepath false
